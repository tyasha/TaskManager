package org.yakov.model;

import org.yakov.util.IdProvider;

public class BasicTask extends Task{
    public BasicTask(IdProvider idProvider, String description) {
        super(idProvider, description);
    }
}
